# 多设备间歇性超时

## 参数化环境

- 客户端：桌面系统 A 与桌面系统 B
- 接入：同一个本地网络
- 节点：共享同一入口和出口
- 现象：两台设备在相近时间全部超时，另一节点仍可用

## 诊断

共同失败首先指向共享节点、共享 DNS、共享出口或目标，而不是两台客户端各自同时损坏。需要对齐两端时间，比较入口连接、VPS 日志、出站连通性和恢复时间。

重新导入或重启后恢复只说明状态被重置。没有故障时段证据时，不能断定是 CGNAT、热更新、客户端端口或服务器路由。

## 下一步

在客户端和 VPS 安装限时记录，并以同一目标和协议复现一次。
