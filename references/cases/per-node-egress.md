# 节点专属上游出口

## 参数化环境

- VPS 有原生公网出口
- 供应商提供带认证的 SOCKS/HTTP 上游
- 只允许指定节点使用该上游

## 诊断

先确认供应商资源不是绑定在 VPS 网卡上的地址。正确边界是代理内核中的独立 outbound 和精确路由规则，而不是修改系统默认路由。

验证必须同时证明新节点使用新出口、旧节点仍使用原出口、SSH/面板正常、配置测试通过以及回滚可用。

## 下一步

记录变更前每个受影响客户端的实际出口，作为变更后对照。
