# 双栈和 TUN 路径不一致

## 参数化环境

- 节点域名同时返回 A 和 AAAA
- 客户端启用 TUN
- IPv4 与 IPv6 的代理规则或可达性不同

## 诊断

域名双栈不会必然导致故障，但客户端可能按自身策略选择不同地址族。若 TUN 只接管 IPv4，IPv6 可能直连；若面板地址也被送进当前代理节点，可能产生访问回路。

应分别记录 A/AAAA、IPv4/IPv6 默认路由、TUN 路由和两个地址族的实际出口。不能仅看到 AAAA 就认定发生绕过。

## 下一步

在相同时间分别执行强制 IPv4 和强制 IPv6 测试，比较入口和出口。
